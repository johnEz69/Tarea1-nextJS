import { Component } from "react"; //importamos una clase de React que se llama component




export default class ComponenteDeClase extends Component{
render (){ //usamos el metodo render para que renderice y retorne desde una clase
return( <p>hola Mundo</p>)

}
}